# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-5.8.0
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2026-03-16T05:20:16Z"
  labels:
    app: lovingtaussig-pod
  name: lovingtaussig-pod
spec:
  containers:
  - args:
    - bash
    env:
    - name: TERM
      value: xterm
    image: quay.io/nixroxursox/haproxy:latest
    name: lovingtaussig
    securityContext: {}
    stdin: true
    tty: true

